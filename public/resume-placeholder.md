# Drop your resume here

Place your resume PDF in this `public/` folder named **`resume.pdf`**.

The "Resume" buttons across the site link to `/resume.pdf`, so once the file
exists it will download/open automatically — no code changes needed.

You can delete this placeholder file afterward.
